# Copy to .env and fill in (never commit .env)
# Binance — USDT-M perpetuals (use https://www.binance.com/en/futures/BTCUSDT API keys from Futures wallet)
BINANCE_API_KEY=
BINANCE_API_SECRET=
# BINANCE_TESTNET=true

# OKX — swap / perpetuals (API key + secret + passphrase)
OKX_API_KEY=
OKX_API_SECRET=
OKX_PASSPHRASE=
# OKX_TESTNET=true

# Bybit — linear USDT perpetuals
BYBIT_API_KEY=
BYBIT_API_SECRET=
# BYBIT_TESTNET=true
